"""Returns: coming in a later build."""

import streamlit as st

from ui import configure_page

configure_page("Returns", "What do returns actually cost, and where?")
st.info("This page hasn't been built yet.")
